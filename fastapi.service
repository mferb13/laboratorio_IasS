[Unit]
Description=FastAPI Backend
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt
ExecStart=/opt/venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
